/** @type {import('next').NextConfig} */
const nextConfig = {
  // Enable access from local network for mobile testing
  // Next.js 13.4+ automatically allows network access when --hostname is set
  // or when accessing via network IP
}

module.exports = nextConfig